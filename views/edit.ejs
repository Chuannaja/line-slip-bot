<div class="card shadow-sm">
  <div class="card-header">✏️ แก้ไขข้อมูลการชำระเงิน #<%= payment.id %></div>
  <div class="card-body">
    <form method="POST" action="/admin/edit/<%= payment.id %>">
      <div class="mb-3">
        <label class="form-label">ชื่อ</label>
        <input type="text" name="first_name" value="<%= payment.first_name || '' %>" class="form-control">
      </div>
      <div class="mb-3">
        <label class="form-label">นามสกุล</label>
        <input type="text" name="last_name" value="<%= payment.last_name || '' %>" class="form-control">
      </div>
      <div class="mb-3">
        <label class="form-label">ที่อยู่</label>
        <input type="text" name="address" value="<%= payment.address || '' %>" class="form-control">
      </div>
      <div class="mb-3">
        <label class="form-label">เบอร์โทรศัพท์</label>
        <input type="text" name="phone" value="<%= payment.phone || '' %>" class="form-control">
      </div>
      <div class="mb-3">
		  <label class="form-label">รายการชำระ</label>
		  <select name="payment_type" class="form-select" required>
			<%- include('partials/payment_options', { payment }) %>
		  </select>
		</div>

      <div class="mb-3">
        <label class="form-label">จำนวนเงิน</label>
        <input type="number" step="0.01" name="amount" value="<%= payment.amount || '' %>" class="form-control">
      </div>
      <button type="submit" class="btn btn-primary">💾 บันทึกการแก้ไข</button>
      <a href="/admin/dashboard" class="btn btn-secondary">ยกเลิก</a>
    </form>
  </div>
</div>
